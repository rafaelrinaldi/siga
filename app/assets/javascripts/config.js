define({
  //>>includeStart("offline", pragmas.offline);
  debug: true,
  //>>includeEnd("offline");

  defaultCoordinates: {
    latitude: -23.543972,
    longitude: -46.65843
  },

  defaultMapOptions: {
    mapTypeControl: false,
    panControl: false,
    streetViewControl: false,
    zoom: 13,
    // zoomControl: false
  }
});
